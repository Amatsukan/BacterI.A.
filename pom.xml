<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.codingame.game</groupId>
    <artifactId>bacter-ia</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <exec.mainClass>Main</exec.mainClass>
        <!-- Guice 4 / cglib (gameengine 4.2) needs this on JDK 17+; harmless on JDK 11. -->
        <surefire.argLine>--add-opens java.base/java.lang=ALL-UNNAMED --add-opens java.base/java.util=ALL-UNNAMED</surefire.argLine>
    </properties>

    <dependencies>
        <!-- CodinGame Core Engine -->
        <dependency>
            <groupId>com.codingame.gameengine</groupId>
            <artifactId>core</artifactId>
            <version>4.2.0</version>
        </dependency>

        <!-- Graphic Entity Module -->
        <dependency>
            <groupId>com.codingame.gameengine</groupId>
            <artifactId>module-entities</artifactId>
            <version>4.2.0</version>
        </dependency>

        <!--
            MultiplayerGameRunner for local agents; also supplies Exporter for CodinGame CI.
            Compile scope so export/classpath steps see runner (test scope breaks Exporter).
        -->
        <dependency>
            <groupId>com.codingame.gameengine</groupId>
            <artifactId>runner</artifactId>
            <version>4.2.0</version>
        </dependency>

        <!-- Testes unitários -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                In-process: mvn test-compile exec:java
                Forked replay: mvn test-compile exec:exec@forked-local
                Boss vs Boss: mvn test-compile exec:exec@forked-local -Dexec.mainClass=BossVsBoss
                  Optional JVM props: -Dpython.exe=/path/to/python (else PYTHON_EXE env, else python/python3).
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <!--
                    exec:java usa só mainClass + classpathScope.
                    Não misturar aqui <arguments> com <classpath/>: o goal java falha ao
                    desserializar (element type mismatch).
                    Simulação com JVM filha: exec:exec@forked-local (scripts run-local.bat).
                -->
                <configuration>
                    <mainClass>${exec.mainClass}</mainClass>
                    <classpathScope>test</classpathScope>
                </configuration>
                <executions>
                    <execution>
                        <id>forked-local</id>
                        <configuration>
                            <executable>java</executable>
                            <classpathScope>test</classpathScope>
                            <arguments>
                                <argument>--add-opens</argument>
                                <argument>java.base/java.lang=ALL-UNNAMED</argument>
                                <argument>--add-opens</argument>
                                <argument>java.base/java.util=ALL-UNNAMED</argument>
                                <argument>-classpath</argument>
                                <classpath/>
                                <argument>${exec.mainClass}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Necessário para o JUnit 5 funcionar com mvn test -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.2</version>
                <configuration>
                    <argLine>${surefire.argLine}</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>